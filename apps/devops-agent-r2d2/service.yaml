apiVersion: v1
kind: Service
metadata:
  name: devops-agent-r2d2
  namespace: default
spec:
  selector:
    app: devops-agent-r2d2
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
